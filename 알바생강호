n = int(input())
tmp= 0
result = 0
tip = []


for _ in range(n):
    tip.append(int(input()))

tip = sorted(tip, reverse=True) # list를 정렬할 때 얕은 복사가 된다는 점을 기억해두자.

for i in range(len(tip)):
    tmp = tip[i] - ( (i+1) -1)
    if tmp < 0:
        tmp = 0
    result += tmp

print(result)
